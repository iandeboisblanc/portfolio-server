{
  "name": "portfolio-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "nodemon dist/index.js --exec babel-node",
    "build": "rm -rf dist && babel src -d dist && cp package.json dist/package.json && cp -R evolution dist/evolution && cp -R portfolio dist/portfolio",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ian deBoisblanc",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.24.0",
    "babel-preset-es2015": "^6.24.0",
    "babel-preset-stage-2": "^6.22.0",
    "babel-register": "^6.22.0",
    "express": "^4.15.4"
  }
}
